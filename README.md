Install the environment using `pip install requirements.txt`.

Then run `python run_final_8b_full.py` to start the LLama-8B experiment.

Run `python run_final_70b_full.py` to start the LLama-70B experiment.
